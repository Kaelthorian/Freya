diff --git a/tests/test_integration_grounding.py b/tests/test_integration_grounding.py
--- a/tests/test_integration_grounding.py
+++ b/tests/test_integration_grounding.py
@@ -345,1 +345,1 @@
-                    self.assertTrue(event["fallback"])
+                    self.assertTrue(json.loads(event["payload_json"])["fallback"])
@@ -351,1 +351,1 @@
-        self.store.update_agent(self.agent["id"], normalize_agent({**self.agent, "enabled": False}))
+        self.store.update_agent(self.agent["id"], normalize_agent({"name": "Worker", "role": "Engineer", "enabled": False}))
@@ -420,1 +420,1 @@
-        self.store.update_agent(self.agent["id"], normalize_agent({**self.agent, "enabled": False}))
+        self.store.update_agent(self.agent["id"], normalize_agent({"name": "Worker", "role": "Engineer", "enabled": False}))
diff --git a/control_center/integration.py b/control_center/integration.py
--- a/control_center/integration.py
+++ b/control_center/integration.py
@@ -346,1 +346,5 @@
-    evidence_refs.update(catalog)
+    evidence_refs = set(catalog)
+    for task in active_tasks:
+        task["evidence"] = [item for item in task["evidence"] if item["ref"] in catalog]
+        task["verification"]["items"] = [
+            item for item in task["verification"]["items"] if item["ref"] in catalog]
diff --git a/control_center/integration_storage.py b/control_center/integration_storage.py
--- a/control_center/integration_storage.py
+++ b/control_center/integration_storage.py
@@ -263,1 +263,1 @@
-            received_by_id = {task["id"]: task for task in normalized["tasks"]}
+            received_by_id = {task["id"]: task for task in plan["tasks"]}
