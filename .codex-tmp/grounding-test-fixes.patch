diff --git a/tests/test_integration_grounding.py b/tests/test_integration_grounding.py
--- a/tests/test_integration_grounding.py
+++ b/tests/test_integration_grounding.py
@@ -190,4 +190,4 @@
-        for field, value in mutations.items():
+        run, _, _, record = self.commit("needs_work")
+        for field, value in mutations.items():
             with self.subTest(field=field):
-                run, _, _, record = self.commit("needs_work")
                 plan = copy.deepcopy(run["effective_plan"])
@@ -345,1 +345,1 @@
-                    self.assertTrue(event["payload"]["fallback"])
+                    self.assertTrue(event["fallback"])
@@ -351,1 +351,1 @@
-        self.store.update_agent(self.agent["id"], {"enabled": False})
+        self.store.update_agent(self.agent["id"], normalize_agent({**self.agent, "enabled": False}))
@@ -361,1 +361,1 @@
-        selections = self.store.list_agent_selections(run["id"])
+        selections = self.store.get_orchestration(run["id"])["selections"]
